# Lean production dependencies for cloud deployment.
# Uses the built-in NumPy vector store (VECTOR_STORE=simple) instead of ChromaDB,
# keeping the image small and memory low enough for free tiers.

python-docx>=1.1.2
python-pptx>=1.0.2
PyMuPDF>=1.24.0
sqlalchemy>=2.0.0
watchdog>=4.0.0
requests>=2.32.0
python-dotenv>=1.0.0
numpy>=1.26.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
