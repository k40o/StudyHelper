[project]
name = "studygame-backend"
version = "0.1.0"
description = "AI-powered study RPG — backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-v"
